---

 - name: This is a playbook for database
   hosts: db
   vars:
   become: True

   tasks:

    - name: Creating a directory named database
      file: path=/opt/database state=directory


 - name: This is a playbook for webserver
   hosts: web
   vars:
   become: True

   tasks:

    - name: Creating a directory named webserver
      file: path=/opt/database state=directory

